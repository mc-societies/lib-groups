Participant cache
Group cache

first commands

configuration support in shank (from json and yaml)

bridge between catharos stuff and bedrock like data storage

history
permissions between subjects and targets (memory leak?)

statistics

X translations
X relations between groups




Group inactive:
	* If older than t seconds
Member inactive:
    * If all groups are inactive
    * If older than t seconds

Active groups get pre loaded.

Really active group's members get pre loaded
